/* eslint-disable @typescript-eslint/no-unused-vars */
export abstract class Presenter<DTO, Output> {
  protected constructor() {}
  /* Implement the methods as needed */
  // abstract toJSON(dto: DTO): Output
  // abstract toCSV(dto: DTO): string
}
