import { type OpenAPIV3_1 } from 'openapi-types'

export const filterOffsetParams: OpenAPIV3_1.ParameterObject = {
  name: 'filter[offset]',
  description: 'Offset',
  in: 'query',
  required: false,
  example: 1,
}
